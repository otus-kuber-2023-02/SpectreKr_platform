apiVersion: v1
kind: PersistentVolume
metadata:
  name:  "backup-{{ name }}-pv"
  labels:
    pv-usage: "backup-{{ name }}"
spec:
  persistentVolumeReclaimPolicy: Retain
  accessModes:
    - ReadWriteOnce
  capacity:
    storage: {{ storage_size }}